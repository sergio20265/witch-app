/**
 * Фон экрана теперь рисует AppShell (единый слой вне скролла — так WebView не
 * перерисовывает картинку при каждом кадре прокрутки и фон не перекрывает контент).
 * Компонент оставлен как no-op, чтобы не менять разметку экранов.
 */
export function PageBackground(_props: { k: string }) {
  return null;
}
